import { type Options } from 'prettier'

const configPrettier: Options = {
  trailingComma: 'all',
  printWidth: 128,
  tabWidth: 2,
  semi: false,
  singleQuote: true,
  arrowParens: 'always',
}

export default configPrettier
